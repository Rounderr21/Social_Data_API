// Purpose: Exporting the User and Thought models so they can be used in other parts of the application
const User = require('./user');
const Thought = require('./thought');;

module.exports = { User, Thought };